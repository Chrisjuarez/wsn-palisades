# Runtime dependencies for the Streamlit app.
# Streamlit Cloud auto-detects this file and uses pip (not Poetry),
# which avoids the strict version solver that fails on pymoo's alive-progress dep.

# core scientific stack
numpy>=1.26
pandas>=2.1
scipy>=1.11
matplotlib>=3.8

# geospatial
rasterio>=1.3
shapely>=2.0
pyproj>=3.6
affine>=2.4
geopy>=2.4

# solar / weather
pvlib>=0.10

# optimization
pymoo>=0.6

# parallelism
joblib>=1.3
loky>=3.4
tqdm>=4.66

# maps + plotting
folium>=0.15
plotly>=5.18

# IO helpers
openpyxl>=3.1
python-dotenv>=1.0
boto3>=1.34

# web app
streamlit>=1.32
streamlit-folium>=0.18

# install the local wsn_palisades package
.
